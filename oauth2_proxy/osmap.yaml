FreeBSD:
  bin_path: /usr/local/bin/oauth2_proxy
  config_path: /usr/local/etc/oauth2_proxy.cfg
  user: oauth2_proxy
  workdir: /var/run/oauth2_proxy
  root_group: wheel
  config:
    global:
      - pid: run/php-fpm.pid
      - include: /usr/local/etc/php-fpm.d/*.conf
